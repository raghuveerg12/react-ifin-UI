to run the IFIN application need to install the NODE JS in the system
Once the installation is DONE, GO The Project folder through Command Line and enter npm start command, thus the application will up and start running